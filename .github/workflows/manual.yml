on:
    repository_dispatch:
        types: [incident_report]

jobs:
    handle_incident:
        runs-on: ubuntu-latest
        steps:
            - name: Print incident report
              run: echo "Incident reported"
