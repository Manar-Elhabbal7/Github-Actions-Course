name : first workflow

on:
  push:
    branches: 
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest
    
    steps:
      - name : Print Welcome Message
        run : echo "Welcome to my Workflow! :) "

   
